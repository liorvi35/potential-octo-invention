services:
  - type: web
    name: chess-api
    env: docker
    plan: free
    region: oregon
    dockerfilePath: chess.Dockerfile
    dockerContext: .
    dockerCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    healthCheckPath: /health
    autoDeploy: true

    envVars:
      - key: CORS_ORIGINS
        value: *
      - key: BORDER_SIZE
        value: "16"
      - key: WATERMARK_TEXT
        value: boardClassification
      # Optional, but fine:
      - key: PYTHONUNBUFFERED
        value: "1"
